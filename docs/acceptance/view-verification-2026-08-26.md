# 视图全量验证记录（2026-08-26）

环境：macOS，Tauri dev 窗口 + Playwright（注入只读 IPC mock，数据来自
`~/.boss-jarvis/data` 真实缓存；写命令一律被 mock 拒绝，防止误触真实操作）。

## 结果总览

- 39 PASS / 0 FAIL / 10 INFO / 4 SKIP
- 10 个侧栏分区 + 系统配置 + 确认中心全覆盖。

## 通过项

- 壳层：导航 72 / 热区 52×52 / 选中 42×42 / 顶栏 60；10 个导航项。
- 12 分区：面包屑切换、真实数据渲染、左右 28px 留白、顶部对齐全部一致。
- 日历提醒：日程/提醒行点击打开详情弹层，关闭后消失。
- 邮件：列表与「打开回复草稿」入口存在；未执行真实标记已读/回复。
- 设置：三态主题持久化；标题字号 16 生效、恢复默认回到 14。
- Skill 管理：53 个 Skill、启停按钮齐备。
- 确认中心：点击启停后自动进入确认中心，待确认项入队，全选/跳过工具栏齐全。

## SKIP（数据条件，非缺陷）

- OA 待办 / 资金费用：本地无 `oa-todo.json` 缓存，空态展示正确。
- 邮件回复按钮：当前缓存无可回复邮件（需 needsReply 判定）。
- 每周总结 / 审计日志日期切换：mock 内联数据体积限制，下拉为空。

## 已知边界

- 纯浏览器（无 `window.__TAURI_INTERNALS__`）下所有视图退化为「未获取」空态，
  属预期：Tauri IPC 不可用时壳层不虚构数据。
- 真实写操作（OA 审批、Skill 启停执行、邮件标记已读/回复草稿）经 Rust 集成
  测试覆盖命令层；UI 端到端真实执行仍建议人工在实机点一次。
